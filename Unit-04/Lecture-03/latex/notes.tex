\documentclass[11pt]{article}
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{geometry}
\usepackage{amsmath}
\usepackage{booktabs}
\usepackage{graphicx}
\graphicspath{{../images/}}
\usepackage{amssymb}
\usepackage{hyperref}
\usepackage{xcolor}
\usepackage{listings}
\geometry{margin=1in}

\lstset{
  basicstyle=\ttfamily\small,
  keywordstyle=\color{blue},
  commentstyle=\color{gray},
  stringstyle=\color{teal},
  showstringspaces=false,
  columns=fullflexible,
  frame=single,
  framerule=0.2pt
}

\title{Python Programming\\Unit 04 -- Lecture 03 Notes\\Event Handling and Input Validation}
\author{Tofik Ali}
\date{\today}

\begin{document}
\maketitle
\tableofcontents

\section{Lecture Overview}
GUIs are event-driven: the program waits for user actions (clicks, typing) and
responds using callback functions. Good GUI programs also validate inputs so that:
\begin{itemize}
  \item the app does not crash,
  \item stored data remains consistent,
  \item and the user gets clear feedback.
\end{itemize}

\section{Core Concepts}

\subsection{Callbacks with \texttt{command=}}
The simplest event handling in Tkinter is attaching a callback to a Button:
\begin{lstlisting}[language=Python]
def submit():
    print("Submitted")

tk.Button(root, text="Submit", command=submit).pack()
\end{lstlisting}

\subsection{Binding Events with \texttt{bind}}
\texttt{bind} attaches a function to a specific event sequence.

\begin{lstlisting}[language=Python]
def on_key_release(event):
    print("Key:", event.keysym)

entry.bind("<KeyRelease>", on_key_release)
\end{lstlisting}

Common event sequences:
\begin{itemize}
  \item \texttt{<Button-1>} left mouse click
  \item \texttt{<Return>} Enter key
  \item \texttt{<KeyRelease>} after a key is released
  \item \texttt{<Control-s>} Ctrl+S shortcut
\end{itemize}

\subsection{Validation: What and When}
\textbf{What to validate:}
\begin{itemize}
  \item required fields (must not be empty),
  \item numeric fields (age must be a number),
  \item ranges (age 0--120),
  \item patterns (email).
\end{itemize}

\textbf{When to validate:}
\begin{itemize}
  \item on submit (simple and reliable),
  \item while typing (better user experience, but more code).
\end{itemize}

\subsection{Email Validation (Simple Regex)}
For beginner apps, a simple pattern is often enough.
\begin{lstlisting}[language=Python]
import re
EMAIL = r"^[A-Za-z0-9._%+-]+@[A-Za-z0-9.-]+\.[A-Za-z]{2,}$"
\end{lstlisting}
This checks format only. Real-world email validation is more complex.

\subsection{User Feedback}
Good feedback:
\begin{itemize}
  \item explains what is wrong,
  \item tells the user how to fix it,
  \item does not expose technical details.
\end{itemize}

\section{Demo Walkthrough}
\textbf{File:} \texttt{demo/event\_validation\_demo.py}

What to observe:
\begin{itemize}
  \item Button callback used for submission.
  \item Live validation using \texttt{bind} on input fields.
  \item Message boxes for clear feedback.
  \item Data is ``accepted'' only when all fields are valid.
\end{itemize}

\section{Interactive Checkpoints (with Solutions)}

\subsection*{Checkpoint 1 Solution}
\textbf{Question:} When use \texttt{command=} vs \texttt{bind()}?

\textbf{Answer:}
\begin{itemize}
  \item \texttt{command=} is best for button clicks (no event object needed).
  \item \texttt{bind()} is best for key/mouse events and shortcuts (event object needed).
\end{itemize}

\subsection*{Checkpoint 2 Solution}
\textbf{Question:} Why is validation important?

\textbf{Answer:} It prevents wrong data, avoids crashes, and improves user experience.

\section{Practice Exercises (with Solutions)}

\subsection*{Exercise 1: Age Validation Rule}
\textbf{Task:} Accept age only if it is an integer between 0 and 120.

\textbf{Solution:}
\begin{lstlisting}[language=Python]
age_text = age_var.get().strip()
if not age_text.isdigit():
    error = "Age must be an integer."
else:
    age = int(age_text)
    if not (0 <= age <= 120):
        error = "Age must be between 0 and 120."
\end{lstlisting}

\subsection*{Exercise 2: Keyboard Shortcut}
\textbf{Task:} Bind Ctrl+L to clear the form.

\textbf{Solution (idea):}
\begin{lstlisting}[language=Python]
def clear(event=None):
    name_var.set("")
    email_var.set("")
    age_var.set("")

root.bind("<Control-l>", clear)
\end{lstlisting}

\section{Exit Question (with Solution)}
\textbf{Question:} Write one validation rule for Age input.

\textbf{Example answer:} Age must be a non-negative integer (or in a range 0--120).



% BEGIN SLIDE APPENDIX (AUTO-GENERATED)
\clearpage
\section*{Appendix: Slide Deck Content (Reference)}
\noindent The material below is a reference copy of the slide deck content. Exercise solutions are explained in the main notes where applicable.

\subsection*{Title Slide}
\titlepage
  \vspace{0.5em}
  \begin{center}
  \footnotesize Repository: \texttt{https://github.com/tali7c/Python-Programming}
  \end{center}
\subsection*{Quick Links}
\centering
  \textbf{Core Concepts}\hspace{1em}
  \textbf{Demo}\hspace{1em}
  \textbf{Interactive}\hspace{1em}
  \textbf{Summary}
\subsection*{Agenda}
\begin{itemize}
  \item Core Concepts
  \item Demo
  \item Interactive
  \item Summary
\end{itemize}
\subsection*{Learning Outcomes}
\begin{itemize}
    \item Handle button clicks using callbacks
    \item Bind keyboard/mouse events using \texttt{bind}
    \item Validate user input (required fields, numeric, email pattern)
    \item Provide user-friendly feedback (labels, message boxes)
  \end{itemize}
\subsection*{What is an Event?}
\begin{itemize}
    \item An event is a user action: click, key press, mouse move, window close
    \item Tkinter runs an event loop and calls your functions when events occur
  \end{itemize}
\subsection*{Button Callback (\texttt{command=...})}
\begin{lstlisting}[language=Python]
def submit():
    print("Submitted")

tk.Button(root, text="Submit", command=submit).pack()
  \end{lstlisting}
\subsection*{Binding Events (\texttt{widget.bind})}
\begin{itemize}
    \item \texttt{bind} provides an event object with details
    \item Useful for live validation and shortcuts
  \end{itemize}
  \vspace{0.4em}
  \begin{lstlisting}[language=Python]
def on_key(event):
    print("Key:", event.keysym)

entry.bind("<KeyRelease>", on_key)
  \end{lstlisting}
\subsection*{Validation Strategies}
\begin{itemize}
    \item Validate on submit (simplest)
    \item Validate while typing (better UX)
    \item Validation types:
      \begin{itemize}
        \item required field (non-empty)
        \item numeric range (age)
        \item format (email using regex)
      \end{itemize}
  \end{itemize}
\subsection*{Good Validation Feedback}
\begin{itemize}
    \item Show clear messages: what is wrong and how to fix it
    \item Avoid technical tracebacks for end users
    \item Highlight the field or show a status label
  \end{itemize}
\subsection*{Demo: Registration Form with Validation}
\begin{itemize}
    \item File: \texttt{demo/event\_validation\_demo.py}
    \item Demonstrates:
      \begin{itemize}
        \item button callback for submit
        \item live validation using \texttt{bind}
        \item email validation using regex
      \end{itemize}
  \end{itemize}
\subsection*{Checkpoint 1}
\textbf{Question:} When would you use \texttt{command=} vs \texttt{bind()}?
\subsection*{Checkpoint 2}
\textbf{Question:} Why is input validation important in GUI applications?
\subsection*{Think-Pair-Share}
Discuss:
  \begin{itemize}
    \item What is a good error message for an invalid email?
    \item What is a bad error message?
  \end{itemize}
\subsection*{Key Takeaways}
\begin{itemize}
    \item Events drive GUI behavior
    \item \texttt{command=} is great for buttons; \texttt{bind} is great for key/mouse events
    \item Validation improves correctness and user experience
    \item Always provide clear feedback to the user
  \end{itemize}
\subsection*{Exit Question}
Write one validation rule for Age input in a registration form.
% END SLIDE APPENDIX (AUTO-GENERATED)

\end{document}

